# Gold Sniper - سجل التغييرات

## V211 - Three Engines (إعادة بناء معماري)

### 🎯 الهدف
إعادة بناء طبقة التنبيهات بـ **ثلاث محركات معزولة كلياً**، مع الحفاظ على الرسم (boxes / labels / colors) كما هو في V202.

### 1️⃣ TOUCH ENGINE (لمسات الويك فقط)
- إشارات: 🟢 شراء / 🟩 تعزيز شراء / 🔴 بيع / 🟥 تعزيز بيع.
- يعتمد على `high`/`low` فقط — لا يستخدم إغلاق الشمعة.
- المنطقة تحت السعر = شراء، فوقه = بيع (حسب نقطة الوسط).
- أول لمسة = إشارة أساسية، اللمسات التالية = تعزيز.
- إعادة التسليح: فقط بعد لمس منطقة من الجهة المقابلة.
- **معزول تماماً عن** Break Engine و MTF Engine.

### 2️⃣ BREAK ENGINE (إغلاق مؤكد على فريم الشارت)
- إشارات: ⚡ كسر صاعد / ⚡ كسر هابط / ⚡ كسر متتالي / 🔄 انقلاب الدور.
- يتطلب `barstate.isconfirmed` + `close` خارج البوكس.
- الويك لا يُحتسب كسراً.
- إعادة التسليح: فقط عند عودة `close` لداخل البوكس.
- كسر شمعة واحدة لعدة مناطق متتالية = **تنبيه مجمّع واحد** بنطاق `min ⟶ max`.
- Role Flip: عند كسر بعكس آخر اتجاه كسر مسجّل على نفس المنطقة.
- **معزول تماماً عن** Touch Engine و MTF Engine.

### 3️⃣ MTF ENGINE (M15 / H1 / H4)
- إشارات: ⚡ كسر M15 / ⚡ كسر H1 / 🚀 كسر H4.
- إغلاقات HTF مؤكدة فقط (`request.security` مع `[close[1], close[2]]` + `lookahead_on` = anti-repaint).
- **state مستقل لكل فريم** (لا يتشارك مع المحركين الآخرين).
- إعادة التسليح: فقط عند عودة إغلاق HTF لداخل البوكس.
- كسور متعددة المناطق على نفس HTF Bar = **تنبيه مجمّع واحد**.
- **معزول تماماً عن** Touch Engine و Break Engine.

### 🧹 ما تم حذفه (مقارنة بـ V202)
- `zone_states` / `zone_role` / `freeze_main` / `cooldown_bars` / Dynamic Reclaim.
- المرور المزدوج (Pass 1: hit_zones + Pass 2: main loop) لصالح ثلاث حلقات بسيطة منفصلة.
- خلط منطق التعزيز مع الكسر داخل نفس البلوك.

### 🎨 الرسم
بدون أي تغيير: نفس الـ boxes والـ labels والألوان (`upper_c` / `lower_c`).

---

## V202 - Anti-Repaint Build (نسخة مُصلحة)

### 🔴 إصلاحات حرجة

1. **منع Repaint في بيانات MTF**
   - استبدال `request.security(..., close, lookahead_off)` بـ
     `request.security(..., [close[1], close[2]], lookahead_on)`
   - هذا الـ pattern هو القاعدة الذهبية لمنع repaint: نأخذ قيمة الشمعة المُغلقة فعلياً.
   - النتيجة: تنبيهات MTF لن تظهر/تختفي بعد إغلاق الشمعة.

2. **إصلاح منطق Dynamic Reclaim**
   - الكود القديم كان يحفظ آخر منطقة ملموسة فقط في `hit_zone_idx`.
   - في شمعة طويلة تلمس عدة مناطق، كان يفقد منطق Reclaim بعض الحالات.
   - النسخة الجديدة تستخدم مصفوفة `hit_zones` تتتبع كل المناطق الملموسة.

3. **تجميع تنبيهات MTF**
   - الكود القديم كان يطلق تنبيه MTF منفصل لكل منطقة (حتى 10 تنبيهات لشمعة واحدة قوية).
   - النسخة الجديدة تجمع كل الكسور لنفس الفريم/الاتجاه في **تنبيه واحد** بنطاق `min ⟶ max`.

### 🟡 تحسينات متوسطة

4. **لون الـ Box يعتمد على الـ Role**
   - السابق: اللون يعتمد على موقع المنطقة فوق/تحت السعر فقط.
   - الجديد: SUPPORT = أخضر، RESISTANCE = أحمر، NEUTRAL = حسب الموقع (سلوك سابق).

5. **عناوين Z1...Z10 في الإعدادات**
   - الـ checkboxes كانت بدون نص. الآن مرقّمة لسهولة التمييز.

6. **استخدام `timeframe.change()` بدل `ta.change(time(...))`**
   - أوضح وأكثر idiomatic.

7. **Cooldown قابل للتخصيص بين الإشارات**
   - input جديد `cooldown_bars` للتحكم بكم شمعة بين كل إشارة (افتراضي 0 = نفس السلوك السابق).

### 🟢 تنظيف

8. حذف `int(right_p + label_off)` غير الضرورية.
9. حماية إضافية بـ `not na(...)` قبل مقارنات MTF لتفادي تنبيهات وهمية على أول الشارت.

---

## V201 - النسخة الأصلية
- البناء الأولي لمؤشر Gold Sniper مع نظام المناطق والذاكرة و MTF.
