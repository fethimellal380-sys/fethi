# 🎯 استراتيجية Gold Sniper V9 — المرجع الكامل

> **هذا الملف هو المرجع الأساسي للاستراتيجية. أي تطوير على المؤشر يجب أن يحترم القواعد المسجّلة هنا.**

---

## 1. الفكرة الأساسية

- المؤشر يعمل على الذهب (XAUUSD) كأداة أساسية، يقبل أي رمز سعري آخر.
- المتداول يحدّد يدوياً **10 مناطق سعرية** (Top + Bottom).
- كل منطقة = بوكس برقمين.
- **المناطق حيّة دائماً** — تعود للعمل بعد الكسر بنفس الشروط.

---

## 2. تحديد دور المنطقة (Side / Mode)

| موقع السعر بالنسبة للبوكس | الدور | الإشارات المنتظرة |
|----------------------------|------|---------------------|
| **السعر فوق البوكس** | دعم (BUY) | ○ شراء عند لمس Top، 🟩 تعزيز عند لمس Bot |
| **السعر تحت البوكس** | مقاومة (SELL) | ● بيع عند لمس Bot، 🟥 تعزيز عند لمس Top |

**القاعدة الذهبية:**
- كل منطقة **أسفل السعر** = بوكس **شراء**.
- كل منطقة **أعلى السعر** = بوكس **بيع**.

---

## 3. إشارات اللمس (Touch Signals)

### ○ شراء (BUY)
- شمعة تهبط ويلمس **ذيلها (low)** قمة البوكس **(Top)** → ○ BUY فوراً.

### 🟩 تعزيز شراء (REBUY)
- بعد ○ BUY، يتعمّق السعر (في **نفس الشمعة** أو **شمعات لاحقة**) ويلمس قاع البوكس **(Bot)** → 🟩 REBUY فوراً.

### ● بيع (SELL)
- شمعة تصعد ويلمس **ذيلها (high)** قاع البوكس **(Bot)** → ● SELL فوراً.

### 🟥 تعزيز بيع (RESELL)
- بعد ● SELL، يرتفع السعر (في **نفس الشمعة** أو **شمعات لاحقة**) ويلمس قمة البوكس **(Top)** → 🟥 RESELL فوراً.

### 🌟 شمعة واحدة كبيرة تلمس الحدّين (Dual Touch)
- شمعة M15 ضخمة تلمس Top + Bot في نفس اللحظة → ○ BUY + 🟩 REBUY معاً (أو ● SELL + 🟥 RESELL في حالة البيع).

### حساسية اللمس (Offset)
- المتداول يضبط `offset` من الإعدادات.
- شرط اللمس يستخدم `low <= top + offset` (شراء) و `high >= bot - offset` (بيع).

---

## 4. الكسر (Break)

### تعريف الكسر الحرفي
- شمعة **M15** تُغلق **بـ "جزء من جسمها"** خارج المنطقة.
- ليس الذيل وحده، وليس داخل البوكس، الإغلاق هو المعتبَر.
- **هبوط:** `close M15 < bot` → ⚡⬇️ M15 BREAK ●
- **صعود:** `close M15 > top` → ⚡⬆️ M15 BREAK ○

### ما لا يُعتبر كسراً
- ❌ إغلاق داخل البوكس (`bot ≤ close ≤ top`).
- ❌ ذيل يخرج خارج البوكس مع إغلاق داخله.
- ❌ شمعة تسحب السعر خارج البوكس بدون إغلاق M15 خارجه.

### الكسر المتتالي (Multi-Break)
- شمعة M15 واحدة تكسر عدة مناطق متتالية → ⚡⏫ M15 MULTI BREAK ○ أو ⚡⏬ M15 MULTI BREAK ●.
- التنبيه واحد مجمَّع، لا يُكرَّر لكل منطقة.

### تنبيهات الفريمات الأعلى
- **H1**: يجمع كل كسور الساعة الكاملة، يُطلق تنبيهاً واحداً عند إغلاق H1.
- **H4**: يجمع كل كسور الـ 4 ساعات، يُطلق تنبيهاً واحداً عند إغلاق H4.

---

## 5. الدورة الكاملة (Cycle)

```
شراء على المنطقة A
   ↓
السعر يصعد ويلمس المنطقة B (الأعلى)
   ↓
● بيع جديد على B (لأن السعر تحتها) — هذه نهاية صفقة الشراء A يدوياً
   ↓
🟥 تعزيز بيع عند لمس Top(B) إذا تعمّق السعر
   ↓
السعر ينزل ويلمس A مرة ثانية
   ↓
○ شراء جديد على A (دورة جديدة)
```

### نهاية الدورة
- **نجاح الصفقة:** الوصول إلى البوكس المعاكس (TP يدوي).
- **فشل الصفقة (الكسر):** شمعة M15 تُغلق خارج البوكس → الدورة تنتهي ويتبدّل دور المنطقة.

---

## 6. السيناريوهات الخاصة (سيولة كبيرة + شموع سريعة)

### السيناريو: شمعة سريعة تكسر منطقة وتلمس التالية
```
السعر يصعد بسرعة
   ↓
يكسر المنطقة A (إذا أغلقت M15 فوقها) → ⚡⬆️ M15 BREAK ○ على A
   ↓
يلامس المنطقة B (الأعلى) ← في نفس الشمعة أو الشمعة التالية
   ↓
● SELL على B (فوري عند اللمس بالذيل)
   ↓
إذا تعمّق إلى Top(B) → 🟥 RESELL
   ↓
إذا أغلقت M15 فوق B → ⚡⬆️ M15 BREAK ○ على B
إذا انعكس قبل إغلاق M15 → لا تنبيه كسر (لأنه لم يحدث كسر فعلياً)
```

### السيناريو: شمعة كبيرة تكسر A وتنزل مباشرة إلى B الأسفل
- في نفس الشمعة:
  - ⚡⬇️ M15 BREAK ● على A (إذا أغلقت M15 تحت A)
  - ○ BUY على B (لأن B أصبحت أسفل السعر بعد الكسر)
  - 🟩 REBUY إذا تعمّق إلى Bot(B)

### القاعدة الأساسية
> **كل تنبيه يرافقه حدث حقيقي على الشارت — لا تنبيه مفصول عن الواقع.**
> **التنبيه لحظي، غير متأخر، في نفس الثانية مع الحدث.**
> **إذا لم يحدث الكسر فعلياً، لا تنبيه كسر.**

---

## 7. الميزات التقنية الإلزامية

| الميزة | الوصف |
|--------|------|
| **Anti-Repaint** | استخدام `lookahead_off` على بيانات MTF. شمعة M15 تُقرأ بعد إغلاقها فقط. |
| **Side Lock** | `mode` يتبدّل **فقط** بكسر مؤكَّد. لا يتبدّل بذبذبة السعر داخل البوكس. |
| **varip للأقفال** | حماية الحالة (b1/b2/s1/s2) من إعادة الحساب التاريخي. |
| **Reclaim بالسعر** | الكسر يُعيد ضبط حالة المنطقة وتعود للعمل. |
| **Dual Touch Fix** | شمعة كبيرة تلمس Top + Bot → الإشارتين معاً (○ + 🟩 أو ● + 🟥). |
| **اللمس لحظي** | إشارات اللمس تُطلق على شموع الشارت الحية، لا تنتظر إغلاق M15. |
| **الكسر عند الإغلاق** | تنبيه الكسر يُطلق عند إغلاق M15 فقط (تأكيد جسم خارج البوكس). |

---

## 8. إدارة الصفقة (يدوية)

- ربح الهدف: ~10$ لكل صفقة (تقديري).
- خسارة قصوى: ~3$ كحد أقصى.
- **إغلاق الصفقة عند المنطقة المعاكسة** (يدوياً عند الوصول للبوكس التالي).
- **لا يرسم المؤشر TP/SL** — الإدارة بصرية بناءً على المناطق فقط.

---

## 9. المناطق العشر الافتراضية للذهب

| المنطقة | Top | Bottom |
|---------|-----|--------|
| Z1  | 3665 | 3655 |
| Z2  | 3425 | 3415 |
| Z3  | 3300 | 3290 |
| Z4  | 3200 | 3190 |
| Z5  | 3100 | 3090 |
| Z6  | 3000 | 2990 |
| Z7  | 2900 | 2890 |
| Z8  | 2800 | 2790 |
| Z9  | 2700 | 2690 |
| Z10 | 2600 | 2590 |

---

## 10. الإعدادات الافتراضية

| الإعداد | القيمة |
|---------|--------|
| حساسية اللمس (Offset) | 1.0 |
| مسافة الإظهار | 0 (عرض الكل) |
| إزاحة الأرقام | 0 |
| لون الشراء | أخضر |
| لون البيع | أحمر |

---

## 11. تنسيق التنبيهات (إلزامي)

| الحدث | الرسالة |
|-------|---------|
| ○ شراء | `○ BUY {price}` |
| 🟩 تعزيز شراء | `🟩 REBUY {price}` |
| ● بيع | `● SELL {price}` |
| 🟥 تعزيز بيع | `🟥 RESELL {price}` |
| كسر M15 صعود | `⚡⬆️ M15 BREAK ○ [bot ⟶ top]` |
| كسر M15 هبوط | `⚡⬇️ M15 BREAK ● [bot ⟶ top]` |
| كسر M15 متتالي صعود | `⚡⏫ M15 MULTI BREAK ○ [أدنى ⟶ أعلى]` |
| كسر M15 متتالي هبوط | `⚡⏬ M15 MULTI BREAK ● [أدنى ⟶ أعلى]` |
| كسر H1 صعود | `⚡⬆️ H1 BREAK ○ x{n} [low ⟶ high]` |
| كسر H1 هبوط | `⚡⬇️ H1 BREAK ● x{n} [low ⟶ high]` |
| كسر H4 صعود | `⚡⬆️ H4 BREAK ○ x{n} [low ⟶ high]` |
| كسر H4 هبوط | `⚡⬇️ H4 BREAK ● x{n} [low ⟶ high]` |

---

## 12. المبدأ الحاكم (لا يخالَف)

> **"كل تنبيه = حدث حقيقي مرئي على الشارت في نفس اللحظة."**

- التنبيه يحدث فقط عندما يحدث الفعل على الشارت.
- لا تنبيه مع تأخير شمعة.
- لا تنبيه بدون فعل مرئي.
- اللمس لحظي على الذيل، الكسر مؤكَّد على الإغلاق.


---

# 📦 سجل الإضافات على المؤشر

> هذا القسم يوثّق كل تطوير/إصلاح يُضاف فوق نواة الاستراتيجية. كل إضافة لها سبب، آلية، ونتيجة موثّقة.

## ➕ Addition #1 — Realtime Cleanup (تنظيف عند الدخول للزمن الحي)

**التاريخ:** 31 May 2026
**النوع:** إصلاح حرج لنظام التنبيهات

### المشكلة
- دالة `alert()` في Pine Script v5 لا تُطلق على الشمعات التاريخية، فقط على الشمعة الحية (Realtime).
- عند تحميل المؤشر، يُعالج كل الشمعات التاريخية ويضبط حالات اللمس (`b1/b2/s1/s2 = true`) دون إطلاق تنبيهات.
- عند الانتقال للزمن الحي، تكون الحالات مقفولة بـ `true`، فأي لمسة تالية لا تُطلق تنبيهاً (الكود يظنّ أنها مسجَّلة سابقاً).
- النتيجة: المتداول يرى الذيل يلمس البوكس على الشارت لكن لا يصله تنبيه.

### الحل
إضافة علم `varip bool first_rt_done = false` لكل منطقة، وبلوك تنظيف لمرّة واحدة:

```pine
if barstate.isrealtime and not first_rt_done
    b1 := false
    b2 := false
    s1 := false
    s2 := false
    first_rt_done := true
```

### الآلية
1. أثناء المعالجة التاريخية: `barstate.isrealtime = false` → البلوك لا يُنفَّذ.
2. أول تيك حي: `barstate.isrealtime = true` و `first_rt_done = false` → يُصفّر الحالات ويرفع العلم.
3. التيكات الحية اللاحقة: العلم مرفوع → البلوك لا يُنفَّذ مرة ثانية (لا يكسر منطق الإشارات).
4. الذيل الحي يُعاد تقييمه مقابل حدود البوكس → التنبيه يُطلق فوراً عند توافق الشروط.

### الأثر على الاستراتيجية
- **لا يُغيّر** أي قاعدة من قواعد الاستراتيجية.
- **يُكمل** المبدأ الحاكم: "كل تنبيه = حدث حقيقي مرئي على الشارت في نفس الثانية".
- **يحلّ** سيناريو حدوث اللمسة قبل تحميل المؤشر بقليل.

### القيود
- لا يعمل في **Replay mode** (لأن TradingView يعرض الإعادة كحالة تاريخية).
- يعمل تلقائياً عند: تحميل أول، Save & Reload، تغيير الإعدادات.


## ➕ Addition #2 — H1/H4 Break Aggregation Order Fix

**التاريخ:** 31 May 2026
**النوع:** إصلاح منطقي في توقيت تجميع كسور الفريمات الأعلى

### المشكلة
ترتيب البلوكين في تجميع كسر H1 و H4 كان معكوساً:
1. أولاً يُطلق التنبيه عند `h1_new` بقيمة `h1un` القديمة
2. ثم يُصفَّر العداد
3. ثم يُضاف كسر M15 الحالي للعداد

**النتيجة:** عند 10:00 (إغلاق H1)، شمعة M15 الأخيرة (9:45→10:00) تُغلق في نفس tick. كان الكسر فيها يُحسب على الساعة التالية (10-11) بدلاً من الساعة المُغلَقة (9-10).

### الحل
عكس الترتيب — التجميع أولاً، التنبيه ثانياً:

```pine
// أولاً: نضيف كسر M15 الحالي
if up_n > 0
    h1un += up_n
    ...

// ثانياً: عند إغلاق H1 نُطلق التنبيه ثم نصفّر
if h1_new
    if h1un > 0
        alert("⚡⬆️ H1 BREAK ○ ...")
    h1un := 0
```

### الأثر على الاستراتيجية
- يحقّق متطلب الاستراتيجية: "H1 يجمع كسور ساعة كاملة" — **كاملة فعلاً** بما فيها آخر M15.
- نفس الإصلاح طُبّق على H4.
- لا يُغيّر منطق M15 (الذي كان صحيحاً أصلاً).

### القيود
- لا يُؤثّر على البيانات التاريخية (Pine يعيد المعالجة عند كل تحميل).


## ➕ Addition #3 — Zone Freeze System (تجميد المنطقة بعد الإشارة)

**التاريخ:** 31 May 2026
**النوع:** تطوير سلوكي لتقليل ضوضاء التنبيهات

### الحاجة
عندما يُطلق المؤشر إشارة دخول (○ BUY أو ● SELL) على منطقة، الصفقة تكون قائمة. أي تنبيهات إضافية للدخول من نفس المنطقة قبل انتهاء الصفقة هي ضوضاء.

### القاعدة المضافة
1. **بعد ○ BUY أو ● SELL** → تُجمَّد المنطقة (`frozen_dir = "BUY"` أو `"SELL"`)
2. **التعزيز (🟩 REBUY / 🟥 RESELL) يعمل دائماً** — جزء من نفس الصفقة، لا يتأثر بالتجميد
3. **إلغاء التجميد يحدث في حالتين فقط:**
   - **(أ) منطقة عكسية تطلق إشارة:** `g_last_sell_bar > lock_bar` للمنطقة المجمَّدة على BUY (= الصفقة وصلت للهدف).
   - **(ب) كسر M15 يقلب اتجاه المنطقة:** الكسر يُمسح `frozen_dir := na` تلقائياً ليُسمح بإعادة اختبار في الاتجاه الجديد.

### التطبيق التقني
- متغيّرات عامة: `g_last_buy_bar`, `g_last_sell_bar` (تُحدَّث عند كل ○ / ●)
- متغيّرات داخل كل منطقة: `frozen_dir`, `lock_bar`
- شرط الإصدار: `if not b1 and na(frozen_dir) and low <= top + offset` (للشراء)
- التعزيز يبقى بدون شرط `na(frozen_dir)`

### الأثر على الاستراتيجية
- يطابق طلب المستخدم: "تتجمد بالتنبيهات فقط في حالة واحدة، حالة التعزيز لأنها جزء من الصفقة"
- يحقّق المتطلب: "عندما يبتعد السعر في الاتجاه المطلوب ويلمس المنطقة المقابلة، يُزال التجميل"
- يحقّق المتطلب: "تفتح التنبيهات إذا كُسرت المنطقة عكس الاتجاه بشمعة 15 دقيقة لنأخذ صفقات إعادة اختبار"

### القيود
- "المنطقة العكسية" تشمل أي منطقة على الشارت (ليس فقط الأعلى/الأسفل مباشرة) — أي منطقة BUY تُلقي إشارة ● SELL تكفي لإلغاء تجميد منطقة BUY على شراء.
- إذا تكرّرت اللمسة على نفس المنطقة بعد إلغاء التجميد → ستُطلق إشارة جديدة بشكل صحيح.


## ➕ Addition #4 — Trend Continuation Unfreeze (سلاسة مع الترند القوي)

**التاريخ:** 31 May 2026
**النوع:** تطوير سلوكي لمواكبة حركة السوق في الترندات القوية

### الحاجة
في الترند الصاعد القوي:
1. ○ BUY على A → A مجمَّدة
2. السعر **يكسر B الأعلى مباشرة** (لم يلمس B بالذيل قبل الكسر)
3. ⚡ كسر صعود يُطلَق على B
4. **السعر تجاوز الهدف** — الصفقة على A انتهت طبيعياً
5. لكن `g_last_sell_bar` لم يتحدّث (لا توجد إشارة ● SELL)
6. **النتيجة في النسخة السابقة:** A تبقى مجمَّدة ولا تستجيب لإعادة الاختبار

نفس الإشكال في الترند الهابط (مع ● SELL).

### القاعدة المضافة
**إلغاء التجميد له ثلاث حالات الآن (ليس اثنتين):**

1. **منطقة عكسية تطلق إشارة** (سابقاً) — الصفقة وصلت لمنطقة الهدف.
2. **كسر M15 يقلب اتجاه نفس المنطقة** (سابقاً) — تنتظر إعادة اختبار في الاتجاه الجديد.
3. **🆕 كسر M15 على أي منطقة في الاتجاه الموافق للصفقة:**
   - ○ BUY مجمَّد + **كسر صعود** على أي منطقة → السعر تجاوز هدف الشراء → فك التجميد على A.
   - ● SELL مجمَّد + **كسر هبوط** على أي منطقة → السعر تجاوز هدف البيع → فك التجميد على B.

### الإشارات المتوقَّعة في ترند صاعد
```
○ BUY على A
🟩 REBUY على A (إن تعمَّق الذيل)
⚡⬆️ M15 BREAK ○ على A (السعر تجاوز A)  ← يفك تجميد A أيضاً (التجميد لم يعد له معنى)
⚡⬆️ M15 BREAK ○ على B (إذا تجاوز B بدون لمس)  ← يفك تجميد A (الترند تجاوز الهدف)
[إعادة اختبار على B]
○ BUY على B
🟩 REBUY على B
⚡⬆️ M15 BREAK ○ على C
[إعادة اختبار على C]
○ BUY على C
... وهكذا يمشي مع الترند
```

### التطبيق التقني
- متغيّران عامان جديدان: `g_last_up_break_bar`, `g_last_dn_break_bar` (يُحدَّثان عند كل كسر M15).
- شرط الإلغاء: `g_last_sell_bar > lock_bar OR g_last_up_break_bar > lock_bar` (للمناطق المجمَّدة على BUY).

### الأثر على الاستراتيجية
- **يبقى التجميد فعّالاً** ضد الضوضاء داخل النطاق الجانبي (sideways).
- **يُفك التجميد فوراً** عند ترند قوي ليتدفق الإشارات مع السوق.
- **الصفقات المعاكسة** (إعادة اختبار، ● SELL على ترند صاعد) لا تُلغى — الكود يدعمها.
- **التعزيز يبقى دائماً مفتوحاً** بدون تغيير.

### القيود
- الإلغاء يحدث على **أي كسر** M15 بنفس الاتجاه على **أي منطقة**. هذا متعمَّد ليواكب الترند الذي قد يقفز بين مناطق متباعدة.
- إذا أردت لاحقاً تقييد ذلك بـ "الكسر على منطقة في الاتجاه الصحيح فقط" (مثلاً ○ BUY على A يُفك فقط بكسر منطقة B الأعلى من A، لا منطقة C الأقل) أخبرني وأضيفها.


## ⚠️ Addition #4 REVERTED — Trend Continuation Unfreeze (تراجع)

**التاريخ:** 31 May 2026
**النوع:** تصحيح فهم خاطئ لمنطق الاستراتيجية

### السبب
طبَّقت Addition #4 على فرضية أن "كسر منطقة B الأعلى يعني تجاوز هدف A → A تُفك". هذا **فهم خاطئ** للاستراتيجية. المستخدم وضّح:

> "المنطقة المجمَّدة لا تُفك إلا بكسرها هي بنفسها أو بمنطقة عكسية تطلق إشارة. كسر منطقة أخرى لا يعني انتهاء الصفقة على هذه المنطقة."

### المنطق الصحيح للاستراتيجية
- المناطق **حيّة لا تموت**.
- التجميد ينتهي بحدث على **هذه المنطقة بالذات**:
  - كسرها بـ M15 (تتقلب من دعم إلى مقاومة أو العكس)
  - منطقة عكسية تطلق إشارة (الصفقة وصلت للهدف)
- إذا الترند ابتعد كثيراً ثم عاد → المنطقة لا تزال جاهزة (لا حاجة لفك تجميد قسري).

### السيناريو الذي وضّحه المستخدم
```
ترند صاعد:
○ BUY على A → A مجمَّدة على شراء
السعر يصعد ويلمس B الأعلى (B لم تُكسَر بعد) → ● SELL على B (صفقة معاكسة مشروعة)
🟥 RESELL إذا تعمَّق
B تتجمَّد على بيع
─── الآن A مجمَّدة على شراء، B مجمَّدة على بيع ───

السعر يهبط ويلمس A → ⚠️ A لا تطلق ○ BUY لأنها مجمَّدة
لكن: ● SELL على B = منطقة عكسية → A تُفك
→ ○ BUY على A الآن مسموح إذا لمسها مرة أخرى

أو: السعر يكسر A بـ M15 → A تُفك ذاتياً (mode يتقلب)
→ بعد الكسر: إعادة اختبار = ● SELL (لأنها صارت مقاومة)
```

### النتيجة بعد التراجع
- **حُذفت المتغيّرات:** `g_last_up_break_bar`, `g_last_dn_break_bar`
- **حُذف الشرط الإضافي** من إلغاء التجميد
- **التحديث في بلوك الكسر** أُلغي (لم تعد تُحدَّث متغيرات الكسر العامة)
- **القاعدة المُعتمدة الآن:**
  1. منطقة عكسية تطلق إشارة → فك التجميد
  2. كسر M15 على نفس المنطقة → فك التجميد ذاتياً (mode يتقلب → b1/s1 تُمسح → frozen_dir := na)

### الدروس
- **المناطق مستقلة** — كل منطقة لها حياتها الخاصة، لا تتأثر بكسر منطقة أخرى.
- **التجميد محلي** — لا يُلغى إلا بأحداث محلية (هذه المنطقة) أو ربط مباشر بصفقة (منطقة عكسية).
- **الترند يُدار بالكسور المتتابعة** — كل منطقة تُكسَر تستقبل بعدها إعادة اختبار، تتفك تلقائياً عند الكسر بدون حاجة لقواعد إضافية.


---

## 📌 قواعد العمل مع المستخدم (Working Rules)

1. **لا اقتراحات من عند المساعد** — التزام حرفي بتوجيهات المستخدم.
2. **المستخدم يعرف ما يريد** — يُنفَّذ كلامه كما هو دون إضافة "تحسينات" غير مطلوبة.
3. **عند الشك** — يُسأل المستخدم بدلاً من الافتراض.
4. **التطويرات تُسجَّل** كما طلب المستخدم بالضبط، لا أكثر ولا أقل.


---

## ➕ Addition #5 — Reload-Reset (تصفير عند إعادة فتح المؤشر)

**التاريخ:** 31 May 2026
**النوع:** سلوك التهيئة عند إعادة تحميل المؤشر

### القاعدة (من المستخدم)
عند إغلاق المؤشر وإعادة فتحه على الشارت:
- **كل المناطق تُصفَّر** (لا حفظ لتاريخ سابق)
- **التجميد يُلغى** على كل المناطق
- **الحالات** (b1/b2/s1/s2) تُمسح
- **النتيجة:** المؤشر يبدأ من جديد، أي لمسة قادمة بشمعة جديدة تُطلق إشارتها بشكل طبيعي

### التطبيق
البلوك الحالي يحقّق هذا تلقائياً بفضل خاصية `varip` في Pine Script:
- عند إعادة تحميل المؤشر، Pine يعيد بناء كل الحالات `var/varip` من الصفر.
- العلم `first_rt_done` يبدأ `false`.
- عند أول tick حي، البلوك يُصفّر:
  - `b1 := false`
  - `b2 := false`
  - `s1 := false`
  - `s2 := false`
  - `frozen_dir := na`
  - `lock_bar := -1`
  - ثم يضبط `first_rt_done := true`.

### الكود المسؤول (موجود مسبقاً)
```pine
if barstate.isrealtime and not first_rt_done
    b1 := false
    b2 := false
    s1 := false
    s2 := false
    frozen_dir := na
    lock_bar   := -1
    first_rt_done := true
```

### النتيجة
- إعادة تحميل المؤشر = حالة جديدة كاملة.
- لا مناطق مجمَّدة من الجلسة السابقة.
- أول لمسة حية بعد إعادة الفتح → إشارة فورية.


---

## ➕ Addition #6 — Adjacent Zones Cross-Unfreeze (المناطق القريبة)

**التاريخ:** 31 May 2026
**النوع:** إصلاح حرج لمنع تجميد المناطق القريبة عن بعضها

### المشكلة (من المستخدم)
عند وجود منطقتين قريبتين، إشارات اللمس والاقتناص لا تتفاعل بشكل صحيح. منطقة قد تبقى مجمَّدة بسبب منطقة أخرى أطلقت إشارة في نفس الشمعة.

### السبب الجذري في الكود السابق
شرط إلغاء التجميد كان:
```pine
if frozen_dir == "BUY" and array.get(_g_sell_arr, 0) > lock_bar
```

استخدام `>` (أكبر من بصرامة) يفشل في حالة المنطقتين القريبتين:
- شمعة كبيرة تلمس A (○ BUY) و B (● SELL) في نفس الشمعة → كلاهما لهما `bar_index` متساويان
- A.lock_bar = 100، _g_sell_arr = 100
- `100 > 100` = false → A **تبقى مجمَّدة**

علاوة على ذلك: عند إلغاء التجميد سابقاً، كان الكود يُصفّر `b1/b2/s1/s2`، مما قد يسمح بإعادة إطلاق نفس الإشارة على نفس الشمعة (ضوضاء).

### الإصلاح

#### 1. استخدام `>=` بدل `>`
```pine
if frozen_dir == "BUY" and array.get(_g_sell_arr, 0) >= lock_bar
    frozen_dir := na
```
يسمح بإلغاء التجميد المتبادل بين المناطق التي أطلقت إشاراتها في نفس الشمعة.

#### 2. عدم تصفير b1/b2/s1/s2 عند الإلغاء
- الإلغاء يفك التجميد فقط (`frozen_dir := na`)
- إعادة التسليح الفعلية تتم في بلوك `m15_new` عند ابتعاد M15 عن المنطقة
- هذا يضمن أن المنطقة لا تُطلق نفس الإشارة مرتين على نفس اللمسة، لكنها جاهزة لإشارة جديدة بعد ابتعاد السعر فعلياً

### السيناريو الذي يصبح ممكناً الآن
```
شمعة كبيرة تنزل بسرعة:
- تلمس Top(A) → ○ BUY على A         (A.lock_bar = 100)
- نفس الشمعة، السعر يصعد ويلمس Bot(B) → ● SELL على B  (_g_sell_arr = 100)
- في تيك لاحق من نفس الشمعة، A تفحص: _g_sell_arr (100) >= A.lock_bar (100) ✓
  → A تُفك من التجميد
- A جاهزة لإشارة جديدة (لكن b1 لا يزال true → لن تُطلق ○ BUY مكرراً)
- بعد ابتعاد M15 عن A → b1/b2 تُصفَّر → A جاهزة بالكامل لدورة جديدة
```

### الأثر على الاستراتيجية
- **يحفظ التجميد** ضد الضوضاء على نفس المنطقة (b1/s1 latch ما زال يعمل).
- **يفتح المناطق القريبة** للتفاعل مع بعضها كما طلب المستخدم.
- **لا يؤثر** على منطق التعزيز (يعمل دائماً بدون شرط `frozen_dir`).
- **لا يؤثر** على الكسر (يعالج تجميده محلياً في بلوك `if broke`).


---

## ➕ Addition #7 — Active Trade Drawing (رسم الصفقة الناشطة)

**التاريخ:** 31 May 2026
**النوع:** ميزة بصرية جديدة لرسم الصفقة على الشارت

### المتطلبات (من المستخدم)
- رسم **صفقة واحدة فقط** نشطة على الشارت في أي وقت.
- عند فتح صفقة جديدة (شراء أو بيع) → الصفقة السابقة تختفي، الجديدة تأخذ مكانها.
- عناصر الرسم: BUY/SELL، REBUY (عند التفعيل فقط)، TP1، TP2، SL.
- بدون أرقام دوائر على الشموع (تقليل الزحام).

### التركيب البصري

#### حالة الشراء (BUY)
```
━━━━━━ TP2 ━━━━━━━━━━━━━━━━━━━━━━━  (Bot المنطقة المعاكسة الأقرب)
┌──────────────────────────────────┐
│   منطقة زرقاء خفيفة                │
├ ─ ─ TP1 ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─┤  (نصف المسافة)
│                                  │
━━━━━━ BUY ━━━━━━━━━━━━━━━━━━━━━━━  (Top البوكس = Entry)
┌──────────────────────────────────┐
│   منطقة حمراء خفيفة                │
│ ─ ─ REBUY ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─│  (Bot البوكس، يظهر فقط بعد التعزيز)
└──────────────────────────────────┘
━━━━━━ SL ━━━━━━━━━━━━━━━━━━━━━━━  (Bot البوكس = SL)
```

#### حالة البيع (SELL)
نفس البنية معكوسة:
- المنطقة الزرقاء تحت Entry (إلى TP2)
- المنطقة الحمراء فوق Entry (إلى SL)

### القواعد الفنية

| العنصر | القيمة |
|--------|--------|
| Entry | Top البوكس (للشراء) / Bot البوكس (للبيع) |
| SL | Bot البوكس (للشراء) / Top البوكس (للبيع) |
| TP2 | أقرب Bot من منطقة فوق (للشراء) / أقرب Top من منطقة تحت (للبيع) |
| TP1 | منتصف المسافة بين Entry و TP2 |
| REBUY | Bot البوكس (للشراء) / Top البوكس (للبيع) — يظهر فقط بعد 🟩/🟥 |
| الهدف الافتراضي | 30$ عند عدم وجود منطقة معاكسة |

### حالات اختفاء الرسم
1. **ضرب TP2** — `high >= tp2` للشراء، `low <= tp2` للبيع
2. **ضرب SL** — `low <= sl` للشراء، `high >= sl` للبيع
3. **كسر M15 يقلب اتجاه المنطقة** — يُغلق `_trade_active = 0` تلقائياً
4. **فتح صفقة جديدة** — الصفقة الناشطة تُستبدل بالجديدة (الناشطة دائماً واحدة فقط)

### التطبيق التقني
- متغيّرات الصفقة عامة: `_trade_active`, `_trade_dir`, `_trade_zone`, `_trade_entry`, `_trade_sl`, `_trade_tp1`, `_trade_tp2`, `_trade_rebuy`, `_trade_reb_on`, `_trade_bar`.
- مصفوفات أحادية الخلية لتعديلها من داخل `f_zone` (قيود Pine v5).
- كائنات الرسم (`var box`, `var line`, `var label`) مخصّصة مرة واحدة وتُحدَّث إحداثياتها كل bar.
- إخفاء العناصر بـ `color.new(gray, 100)` (شفاف كامل).

### الإعدادات الجديدة
- `show_trade` (default = true) — إظهار/إخفاء رسم الصفقة.
- `default_tp` (default = 30) — الهدف الافتراضي عند عدم وجود منطقة معاكسة.
- `trade_blue` — لون المنطقة الزرقاء (TP).
- `trade_red` — لون المنطقة الحمراء (SL).

### الأثر على الاستراتيجية
- **لا يُغيّر** أي قاعدة من قواعد الاستراتيجية.
- **عناصر بصرية بحتة** — كل المنطق (الكسر، اللمس، التعزيز، التجميد) لم يُمَس.
- يدعم رؤية بصرية للصفقة الناشطة فقط.


---

## 🔍 المراجعة النهائية الجراحية (Final Audit)

**التاريخ:** 31 May 2026
**الحالة:** اكتمل المؤشر — جاهز للاختبار

### نتيجة المراجعة الكاملة سطراً سطراً

#### ✅ تنبيهات اللمس (○ BUY / ● SELL)
- محصورة في `if mode == "BUY"/"SELL"` خارج `if m15_new` → **لحظية** على شمعة الشارت الحية.
- شرط `not b1 and na(frozen_dir)` يمنع:
  - التكرار في نفس الشمعة (`b1` latch)
  - الإصدار بعد التجميد (`frozen_dir`)
- مطابقة 100% للاستراتيجية: لمس بالذيل + تنبيه فوري + لا ضوضاء.

#### ✅ تنبيهات التعزيز (🟩 REBUY / 🟥 RESELL)
- تعمل **دائماً** (لا تتأثر بالتجميد).
- شرط `b1 and not b2` يضمن أن التعزيز لا يُطلق قبل اللمس الأول.
- `b2/s2` latch يمنع التكرار.
- مطابقة 100%: التعزيز جزء من الصفقة، يصل دائماً.

#### ✅ تنبيهات الكسر (M15 / H1 / H4)
- **M15:** محصور في `if m15_new` → يُطلق **مرة واحدة فقط** لحظة إغلاق M15.
- شرط الكسر: `c15 < bot` للهبوط، `c15 > top` للصعود — إغلاق M15 خارج البوكس فقط.
- إغلاق داخل البوكس → ❌ ليس كسراً (مطابق للاستراتيجية).
- ذيل خارج فقط → ❌ ليس كسراً.
- المنفرد vs المتتالي: `else if` يمنع الإطلاق المزدوج.
- **H1/H4:** التجميع قبل الإطلاق (Addition #2) — كل ساعة كاملة محسوبة بدقة.

#### ✅ عدم تعارض التنبيهات فيما بينها
- اللمس على A + الكسر على A: غير ممكن في نفس الشمعة لأن الكسر يُحقَّق فقط عند `m15_new` ويُمسح b1/s1 عند الكسر، فاللمس التالي يحتاج إعادة تسليح.
- اللمس على A + اللمس على B: حدثان مستقلان → تنبيهان مشروعان (سيناريو الشمعة الكبيرة).
- الكسر على A + اللمس على B: حدثان مستقلان → تنبيهان مشروعان (المنطقة المكسورة لم تعد لها صلة بـ B).

#### ✅ التزامن بين التنبيهات والرسم
- ○ BUY → فتح صفقة شراء → الرسم يظهر فوراً (المنطقة الزرقاء فوق Entry، الحمراء أسفل، TP1/TP2/SL).
- ● SELL → فتح صفقة بيع → الرسم ينعكس (الزرقاء أسفل، الحمراء فوق).
- 🟩/🟥 → خط REBUY يظهر فقط على الصفقة الناشطة الحالية.
- TP2/SL hit → الرسم يختفي تلقائياً.
- كسر M15 على منطقة الصفقة → الرسم يختفي.
- صفقة جديدة على منطقة أخرى → السابقة تختفي، الجديدة تظهر.

#### ✅ لا تكرار في التنبيهات
- كل تنبيه له:
  - latch محلي (`b1/b2/s1/s2`)
  - `alert.freq_once_per_bar`
  - شرط `frozen_dir` للدخول الأول
- المنطقة الواحدة لا تُطلق نفس الإشارة مرتين على نفس اللمسة.

### الزائدة الوحيدة المُكتشفة وتم تنظيفها
- `textcolor = chart.bg_color` في تهيئة labels الرسم → استُبدلت بـ `color.white` (يطابق التحديثات اللاحقة في الكود).

### الحالة النهائية
الكود **مطابق 100%** للاستراتيجية بعد كل الإضافات (#1 إلى #7) و **نظيف** بدون زوائد. جاهز للاختبار على الذهب عند افتتاح السوق.


---

## ➕ Addition #8 — Restore Emojis & Fix Drawing Visibility

**التاريخ:** 31 May 2026
**النوع:** إصلاح بصري + تحقق من ظهور الرسم

### التعديلات

#### 1. إرجاع الإيموجات الأصلية للتنبيهات والرسم
| الحدث | قبل | بعد |
|------|-----|-----|
| الشراء | `○ BUY` | **🟢 BUY** |
| البيع | `● SELL` | **🔴 SELL** |
| تعزيز شراء | `🟩 REBUY` | **🟩 REBUY** (لم يتغير) |
| تعزيز بيع | `🟥 RESELL` | **🟥 RESELL** (لم يتغير) |
| كسر صعود | `⚡⬆️ M15 BREAK ○` | **⚡⬆️ M15 BREAK 🟢** |
| كسر هبوط | `⚡⬇️ M15 BREAK ●` | **⚡⬇️ M15 BREAK 🔴** |
| كسر متتالي صعود | `⚡⏫ M15 MULTI BREAK ○` | **⚡⏫ M15 MULTI BREAK 🟢** |
| كسر متتالي هبوط | `⚡⏬ M15 MULTI BREAK ●` | **⚡⏬ M15 MULTI BREAK 🔴** |

#### 2. تحقق من رسم الصفقة
- المنطق سليم: عند ○ BUY يتم ضبط `_trade_active = 1` وكل قيم الصفقة فوراً.
- الرسم يحدث في تيك لاحق ضمن نفس الـ bar.
- في حالة عدم وجود منطقة معاكسة: `f_tp2_buy` يستخدم `default_tp` (افتراضي 30$) → الرسم يظهر بـ TP2 = Entry + 30$.
- إذا لم يظهر الرسم: تأكّد من تفعيل `show_trade` (مفعَّل افتراضياً) وأن المنطقة الناشطة تُحدِّث الصفقة فعلاً (شرط `na(frozen_dir)` في الإصدار الأول).

### ملاحظة للمستخدم
- إذا تم ضبط منطقة على شارت BTC مثلاً وكانت **أعلى منطقة** بدون منطقة فوقها، الـ TP2 يحسب على بعد 30$ افتراضي، ويظهر الرسم.
- إذا حدث اللمس **قبل تحميل المؤشر** (في الجلسة التاريخية): تنبيه ○ BUY قد يصل عند أول tick حي بسبب Realtime Cleanup، ويفتح الرسم لحظتها.


---

## ➕ Addition #9 — Break Alert Format (تنسيق تنبيهات الكسر)

**التاريخ:** 31 May 2026
**النوع:** تعديل بصري على رموز تنبيهات الكسر

### التعديلات

| الحدث | قبل | بعد |
|------|-----|-----|
| كسر M15 صعود | `⚡⬆️ M15 BREAK 🟢` | **🟢⬆️ M15 BREAK** |
| كسر M15 هبوط | `⚡⬇️ M15 BREAK 🔴` | **🔴⬇️ M15 BREAK** |
| كسر M15 متتالي صعود | `⚡⏫ M15 MULTI BREAK 🟢` | **🟢⏫ M15 MULTI BREAK** |
| كسر M15 متتالي هبوط | `⚡⏬ M15 MULTI BREAK 🔴` | **🔴⏬ M15 MULTI BREAK** |
| كسر H1 صعود | `⚡⬆️ H1 BREAK 🟢 x{n}` | **🟢⬆️ H1 BREAK x{n}** |
| كسر H1 هبوط | `⚡⬇️ H1 BREAK 🔴 x{n}` | **🔴⬇️ H1 BREAK x{n}** |
| كسر H4 صعود | `⚡⬆️ H4 BREAK 🟢 x{n}` | **🟢⬆️ H4 BREAK x{n}** |
| كسر H4 هبوط | `⚡⬇️ H4 BREAK 🔴 x{n}` | **🔴⬇️ H4 BREAK x{n}** |

### الفائدة
- اللون (أخضر/أحمر) في **بداية الرسالة** لتمييز اتجاه الكسر بنظرة سريعة.
- الأسهم (⬆️/⬇️ مفرد، ⏫/⏬ متتالي) لتمييز نوع الكسر.
- إزالة ⚡ لتقليل الضوضاء البصرية.
- نفس البنية مطبَّقة على alertcondition للواجهة.

### الأثر على المنطق
- **لا يُغيّر** أي منطق — تعديل رسائل التنبيهات فقط.


---

## 🔄 Addition #9 UPDATED — Final Break Alert Format

**التاريخ:** 31 May 2026

### الشكل النهائي (مطابق للصورة المرجعية)

| الحدث | الرسالة |
|------|---------|
| كسر M15 صعود | `🟢⬆️ M15 BREAK [ low → high ]` |
| كسر M15 هبوط | `🔴⬇️ M15 BREAK [ high → low ]` |
| كسر M15 متتالي صعود | `🟢⏫ M15 MULTI BREAK [ low → high ]` |
| كسر M15 متتالي هبوط | `🔴⏬ M15 MULTI BREAK [ high → low ]` |
| كسر H1 (مفرد/متتالي) | `🟢⬆️/🟢⏫ H1 BREAK [ low → high ]` |
| كسر H1 هبوط | `🔴⬇️/🔴⏬ H1 BREAK [ high → low ]` |
| كسر H4 صعود | `🟢⬆️/🟢⏫ H4 BREAK [ low → high ]` |
| كسر H4 هبوط | `🔴⬇️/🔴⏬ H4 BREAK [ high → low ]` |

### القواعد
- **مفرد** (منطقة واحدة): سهم مفرد ⬆️ / ⬇️
- **متتالي** (منطقتان أو أكثر): سهم مزدوج ⏫ / ⏬
- **اللون**: أخضر (🟢) للصعود، أحمر (🔴) للهبوط في بداية الرسالة
- **الاتجاه**: للصعود `[ أدنى → أعلى ]`، للهبوط `[ أعلى → أدنى ]`
- **الفاصلة**: `→` (سهم بسيط) بدل `⟶`
- **Multiple Range**: المتتالي يستخدم نطاق كامل (أدنى bot إلى أعلى top للصعود، أعلى top إلى أدنى bot للهبوط)
- **f_repeat**: حُذفت من الاستخدام، لكن الدالة باقية في الكود (قد تُستخدم لاحقاً)


---

## ➕ Addition #10 — H1 / H4 Independent Body Breaks

**التاريخ:** 31 May 2026
**النوع:** إعادة تصميم منطق كسر H1 / H4

### الفرق عن النسخة السابقة
| النسخة السابقة | النسخة الجديدة |
|----------------|------------------|
| H1/H4 = تجميع كسور M15 خلال الفترة | **H1/H4 = شمعة H1/H4 المغلقة جسمها يكسر المنطقة** |
| كسر متعدد = عدد M15 المكسورة في الساعة | **كسر متعدد = شمعة H1 واحدة جسمها يكسر عدة مناطق** |

### القاعدة الجديدة
- **عند إغلاق شمعة H1**: نفحص جسم الشمعة (`max/min(open, close)`):
  - إذا `body_top > top` للمنطقة AND الشمعة السابقة لم تكن مكسورة (`close[1] <= top`) → **كسر صعود**
  - إذا `body_bot < bot` للمنطقة AND الشمعة السابقة لم تكن مكسورة (`close[1] >= bot`) → **كسر هبوط**
- **مفرد**: منطقة واحدة مكسورة → `🟢⬆️ H1 BREAK` أو `🔴⬇️ H1 BREAK`
- **متتالي**: منطقتان أو أكثر → `🟢⏫ H1 MULTI BREAK` أو `🔴⏬ H1 MULTI BREAK`

### نفس المنطق على H4
شمعة H4 مغلقة جسمها يكسر منطقة → تنبيه H4 لحظي عند إغلاق H4.

### Anti-Repaint
استخدام `request.security` مع `lookahead_off` + قراءة الشمعة المغلقة `[1]` لضمان عدم إعادة الرسم.

### الأثر على الاستراتيجية
- يطابق طلب المستخدم: "شمعة الساعة لما تكسر تلك المنطقة بجزء من جسمها → تنبيه"
- يطابق "كسر متتالي على الساعة" = شمعة H1 واحدة كبيرة تكسر عدة مناطق دفعة واحدة
- مستقل تماماً عن M15 — كل فريم يفحص شمعته الخاصة


---

## ➕ Addition #11 — H1/H4 alertcondition (واجهة Add Alert)

**التاريخ:** 31 May 2026
**النوع:** إضافة flags + alertcondition لتنبيهات H1/H4

### الحاجة
دالة `alert()` تعمل تلقائياً (لو فعّل المستخدم "Any alert() function call")، لكن **alertcondition** ضرورية لإظهار التنبيهات في قائمة "Add alert" في TradingView كخيارات منفصلة.

### المُضاف
8 flags و 8 alertcondition:

| Flag | alertcondition |
|------|----------------|
| `h1_up_brk_evt` | "H1 BUY BREAK" → 🟢⬆️ H1 BREAK |
| `h1_dn_brk_evt` | "H1 SELL BREAK" → 🔴⬇️ H1 BREAK |
| `h1_up_multi_evt` | "H1 MULTI BUY BREAK" → 🟢⏫ H1 MULTI BREAK |
| `h1_dn_multi_evt` | "H1 MULTI SELL BREAK" → 🔴⏬ H1 MULTI BREAK |
| `h4_up_brk_evt` | "H4 BUY BREAK" → 🟢⬆️ H4 BREAK |
| `h4_dn_brk_evt` | "H4 SELL BREAK" → 🔴⬇️ H4 BREAK |
| `h4_up_multi_evt` | "H4 MULTI BUY BREAK" → 🟢⏫ H4 MULTI BREAK |
| `h4_dn_multi_evt` | "H4 MULTI SELL BREAK" → 🔴⏬ H4 MULTI BREAK |

### الخلاصة الكاملة لمحرّكات الكسر الثلاثة المستقلة

| الفريم | المنطق | المنفرد | المتتالي |
|--------|--------|---------|----------|
| **M15** | جسم شمعة 15 دقيقة المغلقة خارج المنطقة | 🟢⬆️ M15 BREAK / 🔴⬇️ M15 BREAK | 🟢⏫ M15 MULTI BREAK / 🔴⏬ M15 MULTI BREAK |
| **H1** | جسم شمعة الساعة المغلقة خارج المنطقة | 🟢⬆️ H1 BREAK / 🔴⬇️ H1 BREAK | 🟢⏫ H1 MULTI BREAK / 🔴⏬ H1 MULTI BREAK |
| **H4** | جسم شمعة 4 ساعات المغلقة خارج المنطقة | 🟢⬆️ H4 BREAK / 🔴⬇️ H4 BREAK | 🟢⏫ H4 MULTI BREAK / 🔴⏬ H4 MULTI BREAK |

كل محرّك مستقل تماماً. كل واحد يفحص شمعته الخاصة عند إغلاقها.


---

## ➕ Addition #12 — Alert Frequency Fix (إصلاح فيضان التنبيهات)

**التاريخ:** 31 May 2026
**النوع:** إصلاح حرج لتكرار التنبيهات

### المشكلة (من المستخدم)
عشرات تنبيهات الكسر تأتي في ثانية واحدة لحدث واحد.

### السبب الجذري
كل تنبيهات الكسر (M15, H1, H4) كانت تستخدم `alert.freq_once_per_bar`. هذا يعني "مرة واحدة لكل bar **من شارت الـ chart**" — وليس bar المؤشر (M15/H1/H4).

**سيناريو الفيضان:**
- المستخدم يفتح الشارت على M5 أو M1
- شمعة M15 تنغلق → `m15_new = true`
- لكن `m15_new` قد تكون true في عدة تيكات على شارت أصغر
- `alert.freq_once_per_bar` يسمح بإطلاق **في كل bar من شارت M5/M1** الذي يحدث فيه الشرط
- النتيجة: تنبيه يُطلق 3 مرات (شمعة M5) أو 15 مرة (شمعة M1)

### الإصلاح
استبدال `alert.freq_once_per_bar` بـ `alert.freq_once_per_bar_close` في كل تنبيهات الكسر:
- **M15**: 4 تنبيهات (مفرد صعود/هبوط، متتالي صعود/هبوط)
- **H1**: 4 تنبيهات
- **H4**: 4 تنبيهات

### الفرق
| الخاصية | once_per_bar | once_per_bar_close |
|---------|--------------|---------------------|
| متى يُطلق | عند أول tick يحقق الشرط في الـ bar | فقط عند **إغلاق** الـ bar |
| تكرار في realtime | ممكن إذا حدث الشرط مرات متعددة | لا يحدث (ينتظر الإغلاق) |
| مناسب لـ | إشارات اللمس اللحظي | كسور الشمعات المغلقة |

### الأثر على التنبيهات الأخرى
- تنبيهات اللمس (○ BUY / 🟩 REBUY / ● SELL / 🟥 RESELL) لم تتغير → تبقى `alert.freq_once_per_bar` لأنها مقصودة لتعمل لحظياً.
- التعزيز محمي بـ `b2/s2 latch` فلا يتكرر.

### النتيجة
كل كسر = **تنبيه واحد فقط** عند إغلاق الشمعة المعنية، بصرف النظر عن timeframe الشارت.


---

## ➕ Addition #13 — Trade Drawing & Box Settings Fixes

**التاريخ:** 31 May 2026
**النوع:** 4 إصلاحات بصرية حسب طلب المستخدم

### 1. زر تفعيل الهدف الافتراضي (`use_def_tp`)
- **عند التعطيل (افتراضي):** TP2 يأتي من المنطقة المعاكسة الأقرب فقط (إن وُجدت).
- **عند التفعيل:** TP2 = Entry ± `default_tp` (تجاهل المناطق المعاكسة).
- يحل مشكلة التعارض عند وجود منطقة معاكسة + هدف افتراضي يعملان معاً.

### 2. ازاحة الأرقام (`label_off`) محصورة في أرقام البوكسات
- `label_off` يطبَّق فقط على labels المناطق العشر (الأرقام بين العارضتين).
- لا يطبَّق على labels الصفقة (BUY/SELL/REBUY/TP1/TP2/SL) — هذه ثابتة على الحد الأيمن.

### 3. تمديد البوكسات يميناً (`extend_right`)
- إعداد جديد افتراضي = 35 شمعة.
- يطبَّق على البوكسات (المناطق العشر) و رسم الصفقة معاً ليمتدّا للحد الأيمن نفسه.

### 4. بقاء رسم الصفقة عند فتح صفقة جديدة
**المشكلة السابقة:** أي ○ BUY / ● SELL على منطقة أخرى كان يستبدل الصفقة الناشطة → الرسم يختفي قبل ضرب TP2/SL.

**الإصلاح:** شرط `if array.get(_trade_active, 0) == 0` يمنع استبدال صفقة ناشطة. الصفقة الجديدة تُسجَّل **فقط** بعد:
- ضرب TP2 → `_trade_active = 0`
- ضرب SL → `_trade_active = 0`
- كسر M15 على منطقة الصفقة → `_trade_active = 0`

### الأثر على الاستراتيجية
- **التنبيهات تستمر** على كل الإشارات (○ BUY / 🟩 REBUY / ● SELL / 🟥 RESELL).
- **الرسم يبقى ثابتاً** للصفقة الأولى الناشطة حتى ضرب TP/SL/الكسر.
- لا تعارض بين منطقة معاكسة وهدف افتراضي.
- البوكسات والرسم يمتدّان لنفس المسافة على اليمين.


---

## ➕ Addition #14 — Reinforcement Independence from Mode

**التاريخ:** 31 May 2026
**النوع:** إصلاح حرج لتعزيز البيع (RESELL) المفقود

### المشكلة (من المستخدم)
في حالة معيّنة (الصفقة الناشطة BUY على منطقة سفلى، السعر يصعد ويلمس Bot منطقة عليا → 🔴 SELL، ثم يتعمَّق ويلمس Top المنطقة العليا)، تنبيه 🟥 RESELL **لا يصل**.

### السبب الجذري
كان شرط التعزيز محصوراً داخل بلوك `if mode == "SELL"`:
```pine
else if mode == "SELL"
    if not s1 ...  // إصدار ● SELL
    if s1 and not s2 ...  // إصدار 🟥 RESELL
```

إذا حدثت أي من هذه الحالات بين ● SELL و 🟥 RESELL:
- **كسر M15 صعودي على المنطقة** → `mode := "BUY"` → بلوك `mode == "SELL"` لا يُنفّذ → التعزيز يضيع
- التعزيز ينتمي لصفقة بيع نشطة لكنه يُحجَب فقط لأن mode تغيّر

### الإصلاح
نقل التعزيز **خارج** بلوك `if mode`:
```pine
if mode == "BUY"
    if not b1 ...  // إصدار 🟢 BUY (محصور بـ mode)
else if mode == "SELL"
    if not s1 ...  // إصدار 🔴 SELL (محصور بـ mode)

// التعزيز خارج mode — يعمل عند تحقق b1/s1 بصرف النظر عن mode الحالي
if b1 and not b2 and low <= bot + offset
    alert("🟩 REBUY ...")
if s1 and not s2 and high >= top - offset
    alert("🟥 RESELL ...")
```

### المنطق
- **الإشارة الأولى** (○ BUY / ● SELL) تتطلب `mode` صحيح (لأن المنطقة يجب أن تكون في الجهة الصحيحة).
- **التعزيز** جزء من نفس الصفقة، يعتمد على `b1`/`s1` فقط.
- إذا انقلب mode بسبب كسر → الصفقة الأصلية ما زالت ناشطة (b1/s1 = true) → التعزيز يصل عند لمس الحد.

### الأثر
- **يحل مشكلة RESELL المفقود** في السيناريو المذكور.
- **لا يكسر** أي منطق آخر لأن:
  - `b1/s1` تُمسح عند الكسر (`if broke: b1 := false`)
  - `b1/s1` تُمسح عند rearm (`l15 > top + offset`)
  - `b2/s2` latch يمنع التكرار

### النتيجة
التعزيز يصل دائماً عند لمس الحد المعزز، بصرف النظر عن وضع mode للمنطقة في تلك اللحظة.


---

## ➕ Addition #15 — TP2 Fix: Far Edge of Opposite Zone

**التاريخ:** 31 May 2026
**النوع:** إصلاح حساب TP2 (الهدف الثاني)

### المشكلة (من المستخدم)
- 🟢 BUY على المنطقة الخضراء: Top=73520، Bot=73480
- المنطقة المعاكسة الحمراء: Bot=73670، Top=73700
- **المتوقع:** TP2 = 73700 (Top الحمراء، أبعد نقطة)
- **الفعلي:** TP2 = 73670 (Bot الحمراء، أقرب نقطة)

النتيجة: TP1 محسوب من نصف المسافة الخاطئة، والمنطقة الزرقاء تتوقف قبل المنطقة المقابلة.

### السبب الجذري
الكود كان يبحث عن منطقة معاكسة باستخدام `v_b` (Bot) كهدف:
```pine
if z1 and v1t > t + min_gap
    r := math.min(r, v1b)  // كان يأخذ Bot
```

### الإصلاح
استخدام **Top المنطقة المعاكسة** كهدف (أبعد حد):
```pine
if z1 and v1b > t + min_gap     // المنطقة فوق السعر بـ min_gap
    r := math.min(r, v1t)        // الهدف = Top الحمراء (أبعد)
```

نفس المبدأ للبيع: TP2 = **Bot المنطقة المعاكسة** (أبعد نقطة سفلى).

### النتيجة
- **TP2 الآن = أبعد حد للمنطقة المعاكسة** (Top للشراء، Bot للبيع)
- **TP1 = نصف المسافة بين Entry و TP2** (يحسب تلقائياً صحيحاً)
- المنطقة الزرقاء تمتد من Entry إلى TP2 (الحد البعيد للمنطقة المقابلة)
- يمنح فرصة لتنبيه RESELL/REBUY عند الوصول للمنطقة المقابلة

### الأثر
- لا يؤثر على وضع `use_def_tp = true` (الهدف الافتراضي اليدوي يبقى كما هو)
- يطبَّق فقط عند `use_def_tp = false` (المنطقة المعاكسة)


---

## ➕ Addition #16 — H1/H4 Break Timing & Repeat Fix

**التاريخ:** 31 May 2026
**النوع:** إصلاح حرج لتوقيت وتكرار تنبيهات H1/H4

### المشاكل المُكتشفة

#### 1. تأخير 15 دقيقة في التنبيه
- التنبيه يصل في 17:15 بدلاً من 17:00
- السبب: `alert.freq_once_per_bar_close` ينتظر إغلاق bar الشارت M15

#### 2. حساب خاطئ للشمعة السابقة
- كان: `request.security("60", [open, close, close[1]], lookahead_off)`
- `close[1]` المُمرَّرة لـ `request.security` تُحسب في سياق H1 (close قبل bar H1 الحالي)
- ثم نأخذ `[1]` من النتيجة → نشير لشمعتين قبل (خطأ)
- النتيجة: `h1pc` تشير لشمعة H1 خاطئة → كسر يحسب على بيانات قديمة → تكرار

### الإصلاحات

#### 1. استخدام `[2]` بدل `close[1]` في request.security
```pine
// قبل
[..., h1pc_r] = request.security("60", [..., close[1]], lookahead_off)
h1pc = h1pc_r[1]   // = close قبل شمعتين (خطأ)

// بعد
[h1op_r, h1cl_r] = request.security("60", [open, close], lookahead_off)
h1cl = h1cl_r[1]   // close الشمعة H1 المغلقة
h1pc = h1cl_r[2]   // close الشمعة H1 السابقة لها (صحيح)
```

#### 2. إضافة `barstate.isconfirmed` على شرط الإطلاق
```pine
if h1_new and barstate.isconfirmed   // فقط على bar مغلق نهائياً
```
يضمن أن التنبيه يُطلق مرة واحدة بعد إغلاق bar الشارت الذي حدث فيه `h1_new`.

### الأثر
- التوقيت أدق: التنبيه يصل عند إغلاق bar الشارت الذي يلي إغلاق H1 الفعلي.
- لا تكرار: `barstate.isconfirmed` + `freq_once_per_bar_close` معاً يضمنان إطلاق واحد.
- بيانات الشمعة السابقة (للتحقق من "ليس مكسوراً مسبقاً") صحيحة.


---

## ➕ Addition #17 — Final TP/SL Cleanup

**التاريخ:** 31 May 2026
**النوع:** إصلاح TP2/SL/REBUY حسب طلب المستخدم

### التغييرات

#### 1. حذف زر `use_def_tp` و `default_tp` كلياً
- `f_tp2_buy(t)` و `f_tp2_sell(b)` تعتمدان فقط على المنطقة المعاكسة
- إذا لا توجد منطقة معاكسة → الدالة تُرجع `na` → الصفقة لا تُفتح والرسم لا يظهر
- لا fallback افتراضي

#### 2. TP2 = أبعد حد للمنطقة المعاكسة
- شراء: TP2 = **Top** المنطقة فوق (أبعد نقطة)
- بيع: TP2 = **Bot** المنطقة تحت (أبعد نقطة)
- TP1 = نصف المسافة بين Entry و TP2

#### 3. SL = ±3$ من حد البوكس
- شراء: `SL = bot - 3.0` (تحت البوكس)
- بيع: `SL = top + 3.0` (فوق البوكس)
- المنطقة الحمراء تمتد من Entry إلى SL، تشمل البوكس + 3$ إضافية

#### 4. REBUY/RESELL داخل البوكس
- شراء: `REBUY = bot` (Bot البوكس)
- بيع: `RESELL = top` (Top البوكس)
- الخط داخل المنطقة الحمراء (بين Entry و SL)

#### 5. شرط فتح الصفقة
```pine
if array.get(_trade_active, 0) == 0 and not na(tp2v)
```
الصفقة لا تُفتح إلا إذا توجد منطقة معاكسة. التنبيه (○ BUY / ● SELL) يصل دائماً، لكن الرسم لا يظهر بدون منطقة معاكسة.

### الأثر على الاستراتيجية
- يطابق طلب المستخدم: "حد البوكس المقابل = 2 (TP2)، نصف المسافة = 1 (TP1)"
- يطابق طلب المستخدم: "SL فوق/تحت الحد بـ 3 دولار"
- يطابق طلب المستخدم: "REBUY في الحد الأدنى للشراء، RESELL في الحد الأعلى للبيع"


---

## ➕ Addition #18 — Configurable SL Distance + REBUY/RESELL Visible from Start

**التاريخ:** 31 May 2026
**النوع:** إصلاحان حسب طلب المستخدم

### 1. مسافة SL قابلة للتعديل
**السبب:** الذهب يتحرك بدولارات (3$ مناسب)، لكن BTC يتحرك بآلاف (3$ صغير جداً، لا يظهر بصرياً).

**التغيير:**
- إعداد جديد: `sl_dist = input.float(3.0, "مسافة SL خارج البوكس ($)", minval = 0.1, step = 0.5, group = g)`
- الافتراضي 3$ (للذهب)
- للبيتكوين: ضع 100$ أو 200$ حسب التقلب
- الكود: `array.set(_trade_sl, 0, bot - sl_dist)` للشراء، `top + sl_dist` للبيع

### 2. خط REBUY/RESELL يظهر منذ فتح الصفقة
**السبب:** المستخدم يحتاج رؤية أين سيكون التعزيز كخط مرجعي قبل تفعيله.

**التغيير:**
| الحالة | الخط | اللون | Label |
|--------|------|-------|-------|
| الصفقة مفتوحة، التعزيز لم يتفعّل بعد | يظهر بشفافية متوسطة (50%) | أحمر باهت | `REBUY price` أو `RESELL price` (بدون إيموجي) |
| التعزيز مفعَّل | يظهر بقوة كاملة | أحمر صريح | `🟩 REBUY price` أو `🟥 RESELL price` (مع إيموجي) |

### الفائدة
- المتداول يرى مكان التعزيز قبل وقوعه (تخطيط بصري)
- عند تحقق التعزيز، الخط يصبح أوضح + label بإيموجي ليلفت الانتباه


---

## ➕ Addition #19 — TP2 Correction: Nearest Edge of Opposite Zone

**التاريخ:** 31 May 2026
**النوع:** تصحيح حساب TP2 حسب طلب المستخدم

### التصحيح
**شراء:** TP2 = **Bot** (قاع) المنطقة المعاكسة فوق
- الصفقة تنتهي عند **أوّل ملامسة** للمنطقة المقابلة (وليس أبعد حد)
- مثال: 🟢 BUY على 73520، المنطقة المعاكسة 73670-73700 → TP2 = **73670** (Bot)

**بيع:** TP2 = **Top** (قمة) المنطقة المعاكسة تحت
- الصفقة تنتهي عند أول ملامسة للمنطقة المقابلة
- مثال: 🔴 SELL على 73600، المنطقة المعاكسة 73450-73480 → TP2 = **73480** (Top)

### التغيير في الكود
| الحالة | قبل (Addition #15 خاطئ) | بعد (Addition #19) |
|--------|--------------------------|---------------------|
| f_tp2_buy | math.min على `v_iT` (Top) | math.min على `v_iB` (Bot) |
| f_tp2_sell | math.max على `v_iB` (Bot) | math.max على `v_iT` (Top) |

### TP1
يبقى نصف المسافة بين Entry و TP2 (محسوب تلقائياً).

### مثال كامل (شراء)
- Entry = Top البوكس = 73520
- المنطقة المعاكسة فوق = 73670-73700
- **TP2 = 73670** (Bot الحمراء — الحد الأقرب)
- **TP1 = (73520 + 73670) / 2 = 73595**

عند ضرب 73670 → الصفقة تنتهي والرسم يختفي.


---

## ➕ Addition #20 — Surgical Audit Fix (3 Critical Bugs)

**التاريخ:** 31 May 2026
**النوع:** فحص جراحي شامل + 3 إصلاحات حرجة

### العيوب المُكتشَفة بعد الفحص الجراحي

#### 🔴 العيب 1: تكرار اللمس فوراً بعد الكسر
**السبب:** عند الكسر يُمسح `b1/s1 := false` ويُلغى `frozen_dir`. ثم على نفس الـ tick، شرط الإصدار يُقيَّم ويصبح صحيحاً → إشارة جديدة فوراً قبل أن تُغلق شمعة الكسر.

**الإصلاح:** إضافة شرط `can_signal_now`:
```pine
bool can_signal_now = not (m15_new and (up_brk or dn_brk))
if mode == "BUY" and can_signal_now
    if not b1 and na(frozen_dir) and low <= top + offset
        ...
```
**النتيجة:** بعد الكسر، الإشارات تنتظر شمعة جديدة قبل التفعيل.

#### 🔴 العيب 2: تأخير 15 دقيقة في تنبيهات H1/H4
**السبب:** `alert.freq_once_per_bar_close` يجعل التنبيه ينتظر إغلاق bar الشارت M15. عند 17:00 يحدث الكسر لكن TradingView يسجّل التنبيه في bar 17:15.

**الإصلاح:** تغيير 8 تنبيهات H1/H4 إلى `alert.freq_once_per_bar`. شرط `if h1_new and barstate.isconfirmed` يضمن إطلاق واحد فقط.

**النتيجة:** التنبيه يصل في نفس bar 17:00 (تأخير ثوانٍ فقط).

#### 🔴 العيب 3: ترتيب نطاق M15 الهابط غير متسق
**السبب:** المفرد كان `[lo → hi]` (تصاعدي) والمتتالي `[hi → lo]` (تنازلي). تنبيهات H1/H4 كلها `[hi → lo]` — كان M15 المفرد الوحيد الشاذ.

**الإصلاح:** توحيد كل تنبيهات الكسر الهابط على `[hi → lo]` (يطابق اتجاه الحركة).

### الحالة النهائية (20 إضافة)
- اللمس: لحظي ✓ بدون تكرار بعد الكسر ✓
- التعزيز: يعمل دائماً ✓
- الكسر M15: مرة واحدة عند الإغلاق ✓
- الكسر H1/H4: لحظي عند إغلاق الفريم بدون تأخير ✓
- اتساق الترتيب في كل تنبيهات الكسر ✓
